# updates

APP / WebUI update **metadata only** (`stable` / `prerelease` / `ci`).

- Magisk / KSU / APatch still use **GitHub Pages** `update.json`.
- CI binaries live on **`ci-dist`**; prerelease packages on GitHub Releases; stable package URLs usually point at Pages.
