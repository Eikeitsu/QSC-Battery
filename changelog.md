# 更新日志

## Unreleased

## 2026.08.30

- 重新设计热更新实现方案以解决热更新之后服务挂起无法启动导致的一系列问题
- 修复热更新之后停充失效
- 修复热更新之后简介不再刷新

## 2026.08.29

- 修复webui首页电池电量与温度不显示
- 修复状态文案不更新
- 修复充放电曲线数据不更新

## 2026.08.28.3

- **扩展统一电池快照**：Shell、Rust 与 WebUI 统一优先读取 `battery` / `bms` / `soc` 节点并规范化温度，缺项时保留 fallback；Rust 自检新增快照来源、电量、温度、状态和缺失原因诊断
- **增强 Rust 只读事件引擎**：保留 Shell 负责停充决策和节点写入，Rust 增加 `watch` 阈值过滤、事件风暴合并、严格的 `/proc` 包名匹配和 MCA `Not charging` / fake sysfs fixture 覆盖，不直接修改充电节点
- **修复原生等待器失败后的恢复路径**：记录 `netlink` / 超时 / 接收失败原因、返回码和时间；失败时退回轮询并按 30s、60s、300s、900s 递增退避，后续可自动重试恢复，不再永久锁死在单一状态
- **降低服务与历史采样写盘**：增加服务心跳、循环/原生唤醒/简介写入/历史 pending 指标；历史采样先批量写入 pending，新增 `data/diagnostic_on` 可选详细诊断采样，默认不增加高频写盘
- **完善守护版本一致性检查**：远程 manifest 版本改为逐段比较，并强制校验 SHA-256 格式；即使版本号相同但本地文件校验不一致，也可直接重新下载修复
- **改进 WebUI 守护交互**：显示下载阶段和进度、本地/远程版本、文件校验、安装包自带或升级继承来源及等待器失败状态；远程版本异常时提供重新下载与修复操作
- **补齐安装、升级和 CI 契约**：保留守护版本元数据和升级继承状态，加入 fake qscd 服务失败/恢复测试、历史 pending fixture、Rust `fmt` / `clippy -D warnings` / `cargo test` 门禁及独立发布契约检查

## 2026.08.28.2

- **重构免重启更新的安全兜底**：热更新前先将完整模块保存到管理器不会清理的外部副本，复制和关键文件校验全部通过后才清除 `update` / `modules_update`；任一步失败都保留标准重启更新，成功后立即清理副本与 worker，卸载时同步清理遗留临时资源，并支持开机恢复中断的热更新任务

## 2026.08.28

- **修复停充后无法恢复充电（重要）**：完善停充节点恢复和状态清理流程；关闭模块、异常中断或温控 / App 停充持续命中时不会再无提示地长期锁定充电，电量低于 20% 时强制恢复充电，恢复失败会保留告警便于排查
- **补强 K90U / MCA 停充判定**：MCA 接管后即使 `battery/status=Not charging`、`usb/online=0`，也会结合 `present`、充电口类型、VBUS 电压等信号识别仍在插线，避免省电快路径跳过停充评估；开启 `data/debug_on` 后可记录评估结果、MCA 节点路径、请求值与读回值
- **优化免重启热更新体验**：暂存目录复制完成后立即清除更新标记并启动 `hotinstall.sh`，残留清理转为后台处理；增加同模块互斥锁，避免重复拉起收尾作业，兼容管理器或第三方安装器的延迟收尾
- **Rust 守护新增阈值事件过滤与原生进程检测**：`watch` 仅在插拔或接近停充阈值时唤醒 shell，`pkgs` 直接遍历 `/proc/<pid>/cmdline` 检测目标进程；不支持这些子命令时自动退回 shell 实现
- **增加原生守护专用轮询间隔**：新增 `loop_interval_idle_native_sec` 与 `loop_interval_plugged_native_sec`，守护可用时分别控制未插电和插电兜底间隔，事件仍可即时唤醒
- **降低事件守护的定时唤醒与 shell 空转**：接收超时改为按截止时间一次等待，并减少待机快路径中的配置检查与无效简介写入
- **明确 Rust / C 守护的职责边界**：Rust 版继续承载新功能，C 版维持 `wait-event`、`probe`、`selftest` 等轻量能力；WebUI 与文档同步展示能力差异
- **扩充停充决策测试**：增加假 sysfs 回归测试，覆盖 MCA 的 `Not charging` 状态、无 `online` 节点、阈值判定和节点写入结果；生产代码统一支持测试用 sysfs 根目录

## 2026.08.27

### 省电（待机功耗）

- 新增 `power_saver`（默认开）与三档轮询间隔：未插电 30s 且该轮只读 `online` 节点、不再 fork `qsc_switch.sh`，插电离阈值较远 10s，进入阈值附近（默认 3%）回到 `loop_interval_sec`；三档均可在 WebUI 调整
- 主循环不再每轮 `dumpsys battery`：电量/状态/温度优先读 sysfs，缺项才回退，不再把 `system_server` 从 doze 唤醒；配置解析改为一次内建遍历，单轮进程数从约 60 降到个位数
- 去掉每轮无条件写盘：调试日志仅在开启调试时写，`data/loop_sleep` 仅在值变化时写，充放电曲线仅插电期间采样，日常待机对 `charge_history.csv` 零写入
- 按 App 停充改为每 10 秒最多检测一次，且 `ps` 只取一次快照（原先每个包名各跑一遍）
- 新增事件唤醒守护 `bin/qscd`：阻塞在内核 `power_supply` uevent 上，未插电时零定时唤醒、插拔即时响应。提供 Rust 与 C 两套等价实现，由 `native_impl`（`rust` / `c` / `off`）选择，`native_daemon` 可整体关闭；仅 arm64 / armv7，装不上或自检失败自动退回定时轮询

### 停充兼容与修复

- 修复 MCA 机型（红米 K90U 等）完全无法停充：2026.08.25 起停充要求 `status` 为充电中(2)/已满(5)，但这类机型充电由 mca_charger 接管，插电充电时 `battery/status` 也可能一直报 Not charging，导致停充分支一次都进不去。现改为「`status` 明确在充电」或「当前不是本模块停充的状态」二者之一即评估停充，原先防止停充后每轮全量重写开关的目的不变；按 App 停充与电流控制卡在同一门槛上，一并放宽
- 修复按 App 停充后可能反复充断：停充使 `status` 变为 Not charging，原判定跳过前台检测而误判应用已退出并立即恢复充电
- 通用节点停充后校验是否真的停了：写入成功但无效的节点自动回滚并换下一个（MCA 常延迟生效，仅做软复核不回滚）
- 自动跳过策略类节点（`night_charging` / `cool_mode` / `batt_protect` / `adapter_cc_mode` 等），不再扫描与盲写；用户显式配置的 `power_switch` 仍可用，WebUI 手填时给出警告
- 温度停充更及时：插电且温度距停充阈值不足 3°C 时切回最短间隔，不再最多晚一个轮询间隔
- 新增 `power_stop_schedule`：仅在设定时段内触发电量停充（温度停充不受限）
- 新增 `wireless_policy`（same / ignore）、停充校验等待秒数、维持间隔等配置项
- 服务启动时清理旧版本遗留的 `data/startup.log` / `data/debug.log`（老版本每轮追加，可能已积到很大）

### 发布与安装

- 非首次更新支持免重启热更新：无开机挂载类变更时切换模块目录并重拉 `service.sh`，管理器支持热更新接口时优先走该路径；模块简介同步显示热更新状态，不再一直停在「等待重启」
- 修复热更新经常不生效（`modules_update` 残留、模块目录留下 `update` 标记）：目录切换作业改用 `setsid` 脱离安装器会话，不再被安装结束时连带杀掉；等待方式从固定 5 秒改为等暂存目录稳定，避免与管理器写 `update` 标记赛跑，并在覆盖后持续清理残留；覆盖前校验暂存关键文件齐全，中断的暂存不会盖掉正在用的模块；过程记入 `/data/adb/qsc/runtime/diagnostics/hot_update.log`，诊断报告可见
- 发布改为 4 个变体包，脚本内容完全一致，差别只在带哪套守护二进制：主包（无后缀）不带二进制、体积最小，`update.json` 指向它；`-rust` / `-c` 各带一套；`-full` 两套都带，安装时按 `native_impl` 逐个自检选用
- 主包安装时会问一次是否现在联网下载守护（可选 Rust / C），跳过或下载失败都只提示、不中断安装，之后可在 WebUI 重试；升级时已下载的守护会自动保留
- 守护二进制与含 sha256 的 `manifest.json` 随发版托管到模块官网，下载落盘前校验，校验或自检不过一律回滚
- 安装时迁移本次新增的配置项，升级不丢已有设置

### WebUI / Action

- 页面布局重构；冷门项（循环间隔、无线忽略、按 App 硬停充等）默认收起，避免抢主路径
- 概览新增充放电曲线：电量与温度两条线，左轴电量 % / 温度 °C、右轴电流 A，时间刻度可直接读值；放电段合并系统自带电池记录，无需自行采样也能成图，卡片标注其中多少点来自系统
- 新增「显示充放电曲线」开关：关闭时连采样一起停并置灰采样开关；重新打开不自动恢复采样，只提示采样仅影响电流线，由用户自行决定
- 新增「事件唤醒（守护）」卡片：开关 + Rust/C 二选一的下载与切换 + 删除，显示当前实现、来源与自检结果；没有守护文件时开关置灰并说明原因
- 机型节点社区分享：分享文本复制/粘贴，本机预制档与仓库预制档双入口，可从仓库同步到本地缓存（`device-presets.json`），不必等模块发版
- 首选停充开关可视化编辑（`preferred_*` / `reassert`）；命名配置档与导入导出（含 preferred / MCA）；一键清除开关缓存；进阶电流项进表单（重申 / 漂移 / 台阶 / 路径）
- 新增停充持锁（关/自动/开）、电量停充时段、停充失败提示、通知分控（停充 / 恢复 / 失败）与勿扰时段（失败仍通知）
- 日志页新增「会话」视图，按停充→恢复折叠
- 新增快捷开关路径说明与 AccA 等冲突提示
- 测开关改为后台执行并轮询结果，降低管理器超时掐断；Action 音量下：插电快速测开关，未插电则诊断
- 诊断报告新增充电状态判定：`battery/status` 原始值、各 `online` 节点、`data/power_switch` 与 `data/active_switch`

### 工程

- 引入 Vitest：日志会话折叠、策略节点识别、预制档解析、配置钳位等单测，挂入 `npm run check`

## 2026.08.25

- 修复部分机型充电几分钟后反复断电又恢复：停充需同时「插电」且 `status` 为充电中/已满，停充后不再每轮全量盲写开关
- 修复魅族等机型息屏「回充亮屏 → 再断电」死循环：新增 `stop_hold_wakelock`（默认 `auto`，魅族/Flyme 或检测到 MCA 时自动持锁；恢复充电、关闭模块或卸载时释放）
- 改善小米 17、红米 K90 等停充不生效：实时探测 MCA `handle_state`（优先 `mca_business_charger`），直接 echo 且不 `chmod`；补充 `stop_handle_charge`
- 停充更稳：优先写首个成功节点并记录 `active_switch` 单点重申，降低与系统充电服务互抢
- 电流墙与端口 `input_suspend` 改为末位兜底；`night_charging` / `cool_mode` / `mi_charge_enable` 等保留在主列表
- 关闭模块时不再维持停充与持锁；卸载时同步恢复充电并释放 wakelock

## 2026.08.14

- 运行日志：日志增加 INFO/WARN/ERROR/DEBUG 等级；日志页按等级着色，并支持筛选（默认 Info，记住上次选择）
- WebUI：自定义供电开关改为只读预览，点按打开大屏代码编辑弹层；编辑器支持自动换行
- WebUI：机型/系统改为多厂商 getprop 兜底（一加/OPPO/vivo 等），修复空属性导致只显示 Android

## 2026.08.13

- 文档：增加 WebUI 文档与WebUI截图
- WebUI：Tab 切换改用 `replace`，侧滑/虚拟返回可直接退出而非回退上一 Tab
- 支持 `power_switch` 自定义供电开关（config.conf / WebUI）；扩充停充与电流节点兜底列表
- 适配 Android 16：`dumpsys` 电量取首个行首 `level:`；补 `night_charging` / `charging_suspend_battery` / OPPO一加 `mmi_charging_enable`；`list_switch` 扫描失败时保留旧列表
- WebUI：拆分策略 / 我的 / 日志 / 概览默认主题为独立组件，抽离表单与外观逻辑
- WebUI：Tab/主题/旁路等改为 TS `enum`；链接、存储键与主题预设集中配置
- WebUI：Vant 改为按需引入样式，去掉全量 `index.css`

## 2026.08.12

- Action：重新设计Action功能，保证脚本不会被提前终止
- WebUI：增加电池信息展示(健康度，设计容量，真实容量，循环次数)
- WebUI：设计电池图标与充电动画
- WebUI：引入 Pinia，`useAppStore` / `useTheme` 改为 setup store
- WebUI：抽离 `useBatteryInfo` / `useConfigForm` / `useThemePackClass` composable
- 修复 MIUIX 深色 + 莫奈：宿主浅色 `--background` 洗白页面导致文字无对比度

## 2026.08.10

- WebUI 体验优化
- 电流控制：旁路增加总开关（关则隐藏子项；不触发旁路与回补）
- 电流控制：漂移重申逻辑优化，减少频繁读写

## 2026.08.09

- 模块显示名改为「充电控制」，管理器简介随运行状态动态更新
- 新增可选「电流控制」（`current.json`，默认关闭）：模拟旁路、慢充、温控限流、游戏限流；未安装则不写入相关文件，WebUI 不显示入口
- 旁路可按电量、温度或时段自动开启（时段支持跨天）；旁路方式可选模拟写电流或探测本机旁路节点
- 新增兼容模式：与其它快充 / 限流模块同装时，可只保留电量 / 温度停充
- Action 支持实测停充开关并锁定首选节点
- 安装时自动卸载「QSC定量停充」与「QSC定量停充_独立开关版」（不迁移配置）
- WebUI 重构为 Vue 3 底部 Tab 界面；支持默认 / MD3 / MIUIX 三套主题（MIUIX 含莫奈、悬浮底栏与液态玻璃）

## 2026.07.24

- 全新品牌图标：色底横放电池用于模块列表 / 文档导航，竖立加粗浮动标区分深浅主题用于文档 Hero
- WebUI 顶栏与「关于」页接入品牌图标，浮动标随浅色 / 深色主题自动切换；发布包同步打入 `icon.png`

## 2026.07.23

- MCA / K90U 兼容改为按本机节点动态探测，写入 `data/device.profile`，不再对所有机型统一硬编码路径
- 支持在模块管理器执行 Action 做现场诊断：音量上刷新权限与状态（默认），音量下进入诊断菜单，可逐项运行只读诊断或重新探测设备（调试包另含写入类检测）
- 正式包与调试包分离：正式包仅含只读 `diagnose`；`testing` / `diag2` 仅打入 `-debug` 包
- 模块脚本按功能拆分到 `bin/lib/`（util / keys / profile / charge），主循环与安装/Action 共用同一套库

## 2026.07.22.2

- 增强红米 K90U（骁龙8至尊版）兼容：优先写入 MCA `handle_state`，停充期间持续重申，避免系统改回节点导致停充失效

## 2026.07.22

- 修复电量与温控同时触发后提前恢复充电、继而再次停充的问题；现在会记录停充原因，并等待所有条件恢复
- 统一换算不同设备上摄氏度、十分之一度、百分之一度和千分之一度的电池温度，避免误触发温控
- 安装前增加音量键确认，按音量下或 20 秒未确认会安全取消
- 更新同 ID 模块时可选择安全迁移旧配置或使用新版默认配置；迁移时仅保留合法字段和数值
- WebUI 改为可选安装，未安装时核心停充逻辑与 Action 状态检查仍可正常使用
- 重做普通深色配色与卡片层次；莫奈深色改为跟随系统色相 / MD3 token，不再与固定深色共用一套写死色值
- 移除深色顶栏浅色状态栏条，改为依赖 `color-scheme` 与 theme-color 同步状态栏

## 2026.07.21.2

- WebUI「关于」页面新增在线使用文档入口
- 新增文档站更新日志页面，可直接查看历史版本变更
- 发版时优先保留已手写的当前版本日志，缺失时再根据提交记录自动生成
- 将 `changelog.md` 同步部署到 GitHub Pages
- 将 Release 模块 ZIP 镜像到 GitHub Pages，改善模块更新与下载体验
- 发版完成后自动触发 Pages 文档重新构建

## 2026.07.21

- 优化深色模式文字、卡片与分隔线对比度
- 修复深色模式下状态栏时间、电量等信息难以辨认的问题
- 修复开启莫奈取色后手动切换深色模式仍显示浅色配色的问题
- 主题切换后同步页面、系统状态栏与 `color-scheme`

## 2026.07.17

- 修复在线更新：`versionCode` 改为 `yyyyMMdd * 100 + 修订号`（如 `2026071701`）
- 发版规范：首发 `2026.07.17`；同日再发 `2026.07.17.2`
- WebUI：悬浮分页、莫奈开关、卡片紧凑、顶栏固定等

## 2026.07.16

- 仓库更名为 QSC-Battery，模块 id 更名为 `QSC_Battery`
- 安装时若检测到旧版 `QuantitativeStopCharging_switch`，自动卸载旧版（不再迁移配置）
- WebUI：外观主题支持浅色 / 深色 / 跟随系统，同步状态栏 theme-color
- 模块：接入 Magisk / KernelSU 在线更新（updateJson）
